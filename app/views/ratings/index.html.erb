<h1>Ratings</h1>

<ul>
  <% @ratings.each do |rating| %>
    <div class="rating">
      <h2>
        <%= rating.ratable_type %> : <%= rating.ratable.name %>
      </h2>
      <%= rating_stars_for(rating.value) %>
    </div>
  <% end %>
</ul>